# ====================
# APPLICATION SETTINGS
# ====================
spring.application.name=bookAPIPractice2
server.port=9192

# ====================
# DATABASE CONFIG
# ====================
spring.datasource.url=jdbc:mysql://localhost:3306/test
spring.datasource.username=root
spring.datasource.password=Prisha@2024
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.name=test

# ====================
# JPA & HIBERNATE
# ====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ====================
# KAFKA CONFIGURATION
# ====================

# Kafka broker address
spring.kafka.bootstrap-servers=localhost:9092

# Kafka Producer settings
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Kafka Consumer settings
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=*

# Kafka Consumer Group ID
spring.kafka.consumer.group-id=book-group

# Optional: Kafka topic to use (referenced with @Value)
app.kafka.book-topic=bulkjson-topic
